# Environment Variables for Vercel Deployment
# Copy this file to .env.local for local development

# JWT Secret for admin authentication (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Database Configuration
# Note: For Vercel deployment, we use in-memory SQLite
# For production, consider using a persistent database like PostgreSQL or MongoDB
DATABASE_URL=sqlite://:memory:

# Optional: Telegram Bot Configuration (if using Telegram features)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHANNEL_ID=your-telegram-channel-id

# Optional: Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=3
